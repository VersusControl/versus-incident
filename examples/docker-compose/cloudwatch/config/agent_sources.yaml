sources:
  - name: cwlogs-app
    type: cloudwatchlogs
    enable: true
    cloudwatchlogs:
      region: ${CW_REGION}
      log_group_name: ${CW_LOG_GROUP_NAME}
      log_stream_prefix: ${CW_LOG_STREAM_PREFIX}
      filter_pattern: ${CW_FILTER_PATTERN}
      page_size: 500
