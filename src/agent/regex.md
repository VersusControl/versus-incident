# Regex

The regex layer is a fast pre-filter — only matching log lines flow into the miner. Use it to ignore obvious noise and to tag patterns with a rule name.

*(This page is a placeholder — see [Configuration](./configuration.md) for `agent.regex` settings.)*
