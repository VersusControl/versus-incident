# Miner

How the agent groups noisy log lines into stable templates using a Drain-style tree, and why the resulting `pattern_id` is the unit of work for everything downstream.

*(This page is a placeholder — see [Getting Started](./getting-started.md) for the runtime behaviour.)*
